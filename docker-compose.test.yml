version: "2"
services:

  postgresql:
    image: postgres

  app_test:
    build:
      context: .
      dockerfile: Dockerfile.test
    links:
      - postgresql

  container_name: jenkins_app_test_1